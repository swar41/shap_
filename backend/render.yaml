services:
  - type: web
    name: thesis-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    github:
      repo: shap_
      branch: main
